# Test prompt

```
I've created a opencode plugin that should catch and rewrite node pacakge manager commands if they use a different package manager than the project is using, this project uses bun. Can you attempt to run some commands using other package managers so we can test it.
 You MUST announce which the command you're attempting to use before executing the command
```